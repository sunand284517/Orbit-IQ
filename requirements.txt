# OrbitIQ Backend Dependencies
# Vercel reads requirements.txt from project root

fastapi
uvicorn
# We will use the standard torch package. Vercel's Python builder doesn't support --index-url.
torch
transformers
pillow
pulp
pydantic
python-multipart
